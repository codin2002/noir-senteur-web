// Site configuration - toggle maintenance mode here
// When maintenanceMode is true, visitors see the "Under Construction" page
// When false, visitors see the full live site

export const siteConfig = {
  maintenanceMode: true,
};
